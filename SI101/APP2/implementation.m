%% Implémentation.M%% Groupe : LISTE DES MEMBRES DU GROUPE% Pombet, Charles% Peter, Guillaume% Facundo, Frank% Vargas, Veronica%% ----------------------%% Méthode 1 : directe% faire dans la fenêtre de commande :publish('implementation.m')% calcul à l'aide de boucles% traitement 1 / Premier formule proposéeload CO2.mat;P = 12;somme = 0;valeur = [zeros(1,144)];for c = 1:144    for r = -12: 12        if ( (c + r > 0) && (c + r <= 144) )          somme = somme + cco2(c + r);        end             end    if c <= 12      valeur(c) = NaN;    elseif c >= 133      valeur(c) = NaN;    else    valeur(c) = somme/25;    endif        somme = 0;endplot(valeur);%%% on a suprime les 12 premiers valeurs et les 12 dernieres car ces valeurs ne sont pas correctes%  traitement 2%  -------------figure(2)long = length(cco2);    x= cco2';  M = 15;  Y = [];  a = 1;    b = (1/M)*ones(1,M);    debut = mod(long,M);  for n=[long:-1:M];    yn = 0;    for p = [0:M-1]        yp =(1/M)*x(n-p);        yn = yn+yp;    end    Y = [yn, Y];  endplot(Y);%%% % traitement 3yn =  0 + 0.05*cco2(1);for i=2 : length(cco2)  yn = [yn 0.95*yn(i-1)+0.05*cco2(i)];endforplot(yn);%% Méthode 2 : avec la fonction filter% On a eu un filter pass bas A = 1 ;B = ones(1,(2*P+1))/(2*P+1) ;X = filter(B,A,cco2);for r = 1: 24  X(r) = NaN;endX2 = [zeros(1,144)];for r = 1: 144  if r <= 12    X2(r) = NaN;  elseif r<=132    X2(r) = X(r+12);  else    X2(r) = NaN;  endendplot(X2)%% Méthode 2 : avec la fonction filter% faire help filtera = 1;b = (1/M)*ones(1,M);X_filtre = filter(b,a,x);plot(X_filtre,'r');%% Méthode 3 : avec la fonction conv.% nécessite un peu de calcul math. %Le calcul d un filtre RII a l aide d un produit de convolution est difficile%a calculer et n'a donc pas ete impl�mente%% Estimation de y(100)% donnez ci-dessous votre méthode pour estimer la meilleure valeur% possible de y(100)%% Solution de l'APP% ici une synthèse de vos choix pour les paramètres et le traitement.